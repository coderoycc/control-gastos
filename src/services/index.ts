/**
 * Servicios de la aplicación.
 * Cada servicio abstrae la capa de acceso a datos (API, mock, localStorage, etc.).
 */

export { fetchFinancialSummary } from './financialSummaryService';
export type { FinancialSummaryResponse } from './financialSummaryService';
